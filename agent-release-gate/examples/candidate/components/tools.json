{
  "contract_version": "synthetic-tools/1.1",
  "tools": [
    {"name": "records.read", "mode": "execute", "destinations": ["records.internal.test"]},
    {"name": "records.write", "mode": "prepare_only", "destinations": ["records.internal.test"]}
  ]
}
