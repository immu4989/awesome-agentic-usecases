# Synthetic records-agent policy 1.0

Read an approved public record when the task lease is active. Route every write request to a
human records owner. Stop when the lease, policy epoch, destination, or monitor is invalid.
