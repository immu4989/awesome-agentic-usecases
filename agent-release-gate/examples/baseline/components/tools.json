{
  "contract_version": "synthetic-tools/1.0",
  "tools": [
    {"name": "records.read", "mode": "execute", "destinations": ["records.internal.test"]},
    {"name": "records.write", "mode": "human_only", "destinations": ["records.internal.test"]}
  ]
}
