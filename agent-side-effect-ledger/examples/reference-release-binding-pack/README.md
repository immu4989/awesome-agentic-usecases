# AAU side-effect release binding

**EVIDENCE BOUND** · agent `agent:benefit-coordinator` · release `synthetic-benefit-coordinator-0.1.0`

Fully bound consequential relationships: **1/1**

Matrix boundary: `notification-service / send_synthetic_notice / synthetic-benefit-cases/notices/*`

Each adapter binding compares the entrypoint bytes, static-local Python material set, and every digest-only workspace material observed during the matrix run.

## Holds

- No binding holds were found in this public-synthetic reference pack.

The manifest binds these copied bytes and digest-only runtime snapshots. Source paths are declarations; Python-level audit hooks are bypassable observation, not a sandbox. This does not bind the interpreter, installed dependencies, environment, container, or live workload identity. Neither a valid binding nor a passing matrix proves production equivalence, authorization, safety, compliance, certification, deployment approval, or an ATO.
